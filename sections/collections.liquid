{%  capture saludo %} 

<h1>asdasdasdsad</h1>
{{ 'product-1' | placeholder_svg_tag: 'my-custom-class' }}
makyjero hola como estas /tes "sss"
<h4>asdasdasdasd</h4>
<img src="https://cdn.shopify.com/s/files/1/0270/1007/6315/files/hero-image-1.jpg?v=1606820000" alt="">


{% endcapture %}
{{ saludo}}
<div>
    {% for block in section.blocks %}
        {% if block.type == 'custom' %}
            <h2 {{ block.shopify_attributes }}>{{ block.settings.title }}</h2>
        {% endif %}
    {% endfor %}
    
    {% for product in collections.frontpage.products %}
        <div class="product-card">
            {% cycle 'odd', 'even' %}
            <a href="{{ product.url }}">
                <img src="{{ product.featured_image.src | img_url: 'medium' }}" alt="{{ product.featured_image.alt }}">
                <p>{{ product.title }}</p>
                <p>{{ product.price | money }}</p>
            </a>
        </div>
    {% endfor %}
</div>



{% schema %}
{
    "name": "Collections",
    "tag": "article",
    "class": "section-class class-2",
    "limit": 1,
    "settings": [
        {
            "type": "richtext",
            "id": "description",
            "label": "description"
        }
    ],
    "blocks": [
        {
            "type": "custom",
            "name": "Text block",
            "settings": [
                {
                    "type": "text",
                    "id": "heading",
                    "label": "Title",
                    "default": "Collections"
                }
            ]
        }
    
    ],
    "presets": [
        {
            "name": "Collections",
            "category": "Products"
        }
    ]

    
}
{% endschema %}